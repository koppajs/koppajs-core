#!/usr/bin/env sh
set -eu

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pnpm is required for the pre-commit hook." >&2
  exit 1
fi

pnpm run check:docs
pnpm exec lint-staged --config lint-staged.config.js
